info:
  name: Ler livro
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/public/book/{{BOOK_SLUG}}"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: ""

runtime:
  variables:
    - name: BOOK_SLUG
      value: voltei

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
