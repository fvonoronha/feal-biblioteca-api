info:
  name: Explorar editoras
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/publishers-to-explore"
  body:
    type: json
    data: |-
      {
        "pagination":{
          "limit": 1000
        }
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
