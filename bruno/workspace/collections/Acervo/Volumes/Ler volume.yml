info:
  name: Ler volume
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/volume/f3cd46c279x"
  body:
    type: json
    data: |-
      {

      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
