info:
  name: Listar volumes
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/volumes"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
