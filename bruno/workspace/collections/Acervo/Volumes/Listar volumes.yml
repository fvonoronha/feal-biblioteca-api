info:
  name: Listar volumes
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/volumes"
  body:
    type: json
    data: |-
      {
        "filterX":{
          "search":"chico",
          "author":[],
          "category":["1000006"]
        },
        
        "pagination":{
          "limit":24,
          "page": 1,
          "sort":{
            "by":"label",
            "order":"desc"
          }
        }
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
