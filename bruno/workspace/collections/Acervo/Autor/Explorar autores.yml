info:
  name: Explorar autores
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/public/explore-authors"
  body:
    type: json
    data: |-
      {
        "filter":{
          "is_spirit":false
        },
        "pagination":{
          "limit":20
        }
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
