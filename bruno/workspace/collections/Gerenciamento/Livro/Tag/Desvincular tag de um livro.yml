info:
  name: Desvincular tag de um livro
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{URL}}/book/1000001/tag/1000003/unlink"
  body:
    type: json
    data: |-
      {
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
