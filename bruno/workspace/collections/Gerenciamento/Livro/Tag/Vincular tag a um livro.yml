info:
  name: Vincular tag a um livro
  type: http
  seq: 4

http:
  method: PUT
  url: "{{URL}}/book/1000001/tag/1000003/link"
  body:
    type: json
    data: |-
      {
        
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
