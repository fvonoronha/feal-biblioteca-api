info:
  name: Desvincular author de um livro
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{URL}}/book/1000001/author/1000001/unlink"
  body:
    type: json
    data: |-
      {
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
