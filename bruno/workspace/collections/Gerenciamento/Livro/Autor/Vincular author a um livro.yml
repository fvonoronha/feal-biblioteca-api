info:
  name: Vincular author a um livro
  type: http
  seq: 4

http:
  method: PUT
  url: "{{URL}}/book/1000001/author/1000001/link"
  body:
    type: json
    data: |-
      {
        "description":"Codificador"
      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
