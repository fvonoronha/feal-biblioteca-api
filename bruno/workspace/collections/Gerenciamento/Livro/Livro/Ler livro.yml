info:
  name: Ler livro
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/book/o-livro-dos-espiritos"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
