info:
  name: Listar livros
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}/books"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
