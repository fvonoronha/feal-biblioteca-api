info:
  name: Teste
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/gemini/books/enhance/20"
  body:
    type: json
    data: |-
      {

      }
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

runtime:
  variables:
    - name: BOOK_SLUG
      value: autistas-do-alem

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
