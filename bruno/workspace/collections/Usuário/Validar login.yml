info:
  name: Validar login
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/account/validate/login"
  body:
    type: json
    data: |-
      {
        "login":"fvonoronha"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
