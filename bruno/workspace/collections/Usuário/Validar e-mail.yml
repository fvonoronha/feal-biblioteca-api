info:
  name: Validar e-mail
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/account/validate/email"
  body:
    type: json
    data: |-
      {
        "email":"fabio.vitor.noronha@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
