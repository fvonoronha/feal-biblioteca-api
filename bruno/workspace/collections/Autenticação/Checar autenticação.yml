info:
  name: Checar autenticação
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/is-auth"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
